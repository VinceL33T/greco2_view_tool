require "spec_helper"

describe Greco2ViewTool do
  it "has a version number" do
    expect(Greco2ViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end